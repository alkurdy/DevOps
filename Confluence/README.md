# Confluence Intake Staging (DevOps)

This folder contains pre-stage assets for the Confluence XML pipeline.
No import has been run yet.

## Pilot Target
- Space: `JAVA`
- Mode: private by default
- Approval model: manual gate at each phase/wave

## Structure
- `templates/` frontmatter and note templates
- `config/` scoring and taxonomy defaults
- `scripts/` pre-import validation script skeletons

## Important
Raw/intermediate/chunked pipeline directories are gitignored in `DevOps/.gitignore`.
